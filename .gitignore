# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Variable files (may contain secrets); keep the shareable template
*.tfvars
*.tfvars.json
!*.tfvars.example

*.tfrc
.terraformrc

# Keep .terraform.lock.hcl committed (not ignored)
